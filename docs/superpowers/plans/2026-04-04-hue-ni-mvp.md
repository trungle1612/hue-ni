# Hue-ni MVP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the full Hue-ni frontend MVP — 5 screens, static JSON data, Vietnamese only — from a bare Vite boilerplate to a working mobile-first travel app.

**Architecture:** React Router v6 wraps an `AppLayout` shell (BottomNav + Outlet) for 3 tab routes; Details is a standalone full-screen route. All place data is imported from `src/data/places.json`. My Trip state lives in `localStorage` via a `useMyTrip` hook.

**Tech Stack:** React 19, TypeScript, Vite, React Router DOM v6, Vitest + @testing-library/react (hook tests only), plain CSS (co-located per component)

---

## Task 1: Install dependencies, set up testing, clean boilerplate

**Files:**
- Modify: `package.json`
- Modify: `vite.config.ts`
- Modify: `src/App.tsx`
- Modify: `src/App.css`
- Modify: `src/index.css`
- Modify: `CLAUDE.md`
- Create: `src/test-setup.ts`

- [ ] **Step 1: Install react-router-dom and test dependencies**

```bash
npm install react-router-dom
npm install -D vitest @testing-library/react jsdom
```

- [ ] **Step 2: Add test script and configure Vitest in vite.config.ts**

Replace `vite.config.ts` with:

```ts
/// <reference types="vitest" />
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: './src/test-setup.ts',
  },
})
```

- [ ] **Step 3: Create test setup file**

Create `src/test-setup.ts`:
```ts
// Global test setup — add any shared mocks here
```

- [ ] **Step 4: Add test script to package.json**

In `package.json`, add to `"scripts"`:
```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 5: Replace App.tsx with a placeholder (will be replaced in Task 5)**

```tsx
function App() {
  return <div>Hue-ni</div>
}

export default App
```

- [ ] **Step 6: Clear App.css**

Replace `src/App.css` with an empty file (single newline). We will add component styles co-located later.

- [ ] **Step 7: Update CLAUDE.md**

Add to the `## Architecture` section:

```markdown
## App Architecture

- Routing: React Router v6 (`react-router-dom`)
- Static data: `src/data/places.json` (imported directly, no API)
- Images: place `public/images/<place-id>-cover.jpg`, `public/images/<place-id>-1.jpg`, etc.
- My Trip state: `localStorage` key `hue-ni-trip` via `src/hooks/useMyTrip.ts`
- Tests: Vitest + @testing-library/react (hook logic only). Run: `npm test`
```

- [ ] **Step 8: Verify dev server runs**

```bash
npm run dev
```

Expected: Vite dev server starts, browser shows "Hue-ni".

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "chore: install react-router-dom and vitest, clean boilerplate"
```

---

## Task 2: TypeScript types

**Files:**
- Create: `src/types/index.ts`

- [ ] **Step 1: Create types**

Create `src/types/index.ts`:

```ts
export type Category =
  | 'tomb'
  | 'landmark'
  | 'cafe'
  | 'food'
  | 'homestay'
  | 'service'

export interface Place {
  id: string
  name: string
  category: Category
  tags: string[]
  coverImage: string
  gallery: string[]
  address: string
  coordinates: { lat: number; lng: number }
  rating: number
  priceRange: '₫' | '₫₫' | '₫₫₫'
  hours: { open: string; close: string }
  vibe: string
  description: string
  insiderTips: string[]
  collection?: string
}

export interface Collection {
  id: string
  title: string
  description: string
}

export interface PlacesData {
  places: Place[]
  collections: Collection[]
}
```

- [ ] **Step 2: Verify TypeScript compiles**

```bash
npm run build
```

Expected: Build succeeds with no type errors.

- [ ] **Step 3: Commit**

```bash
git add src/types/index.ts
git commit -m "feat: add TypeScript types for Place, Collection, Category"
```

---

## Task 3: Static places data

**Files:**
- Create: `src/data/places.json`
- Create: `public/images/.gitkeep`

- [ ] **Step 1: Create the static data file**

Create `src/data/places.json`:

```json
{
  "places": [
    {
      "id": "khai-dinh-tomb",
      "name": "Lăng Khải Định",
      "category": "tomb",
      "tags": ["lăng tẩm", "kiến trúc", "di sản"],
      "coverImage": "https://picsum.photos/seed/khai-dinh/800/500",
      "gallery": [
        "https://picsum.photos/seed/khai-dinh-1/800/500",
        "https://picsum.photos/seed/khai-dinh-2/800/500",
        "https://picsum.photos/seed/khai-dinh-3/800/500"
      ],
      "address": "Khải Định, Hương Thủy, Thừa Thiên Huế",
      "coordinates": { "lat": 16.3943, "lng": 107.6513 },
      "rating": 4.8,
      "priceRange": "₫",
      "hours": { "open": "07:00", "close": "17:30" },
      "vibe": "Kiến trúc Art Deco kết hợp truyền thống — độc nhất vô nhị.",
      "description": "Lăng Khải Định là một kiệt tác kiến trúc kết hợp giữa phong cách phương Đông và phương Tây, được xây dựng từ năm 1920 đến 1931. Nơi đây thờ vua Khải Định, vị vua thứ 12 của triều Nguyễn.",
      "insiderTips": [
        "Đến trước 8h sáng để tránh đám đông và ánh nắng gắt.",
        "Hỏi bảo vệ để xem phòng tranh khảm trai bên trong — thường không mở cho khách vãng lai.",
        "Mang theo áo khoác nhẹ, bên trong khá mát dù trời nóng."
      ],
      "collection": "echoes-of-dynasty"
    },
    {
      "id": "hue-citadel",
      "name": "Đại Nội Huế",
      "category": "landmark",
      "tags": ["hoàng thành", "di sản UNESCO", "lịch sử"],
      "coverImage": "https://picsum.photos/seed/hue-citadel/800/500",
      "gallery": [
        "https://picsum.photos/seed/citadel-1/800/500",
        "https://picsum.photos/seed/citadel-2/800/500",
        "https://picsum.photos/seed/citadel-3/800/500"
      ],
      "address": "Thuận Thành, Thành phố Huế, Thừa Thiên Huế",
      "coordinates": { "lat": 16.4700, "lng": 107.5779 },
      "rating": 4.9,
      "priceRange": "₫",
      "hours": { "open": "07:30", "close": "17:30" },
      "vibe": "Trái tim của cố đô — nơi lịch sử 400 năm vẫn còn thở.",
      "description": "Đại Nội Huế là quần thể kiến trúc cung đình rộng lớn của triều Nguyễn, được UNESCO công nhận là Di sản Văn hóa Thế giới năm 1993. Bên trong là Hoàng Thành, Tử Cấm Thành và hàng trăm công trình lịch sử.",
      "insiderTips": [
        "Thuê xe đạp để khám phá toàn bộ khuôn viên — đi bộ sẽ rất mệt.",
        "Vào buổi chiều tà, ánh vàng chiếu qua Ngọ Môn tạo ra cảnh tượng huyền ảo.",
        "Khu Duyệt Thị Đường thường vắng khách nhưng rất đẹp — đừng bỏ qua."
      ],
      "collection": "echoes-of-dynasty"
    },
    {
      "id": "tu-duc-tomb",
      "name": "Lăng Tự Đức",
      "category": "tomb",
      "tags": ["lăng tẩm", "thơ ca", "hồ sen"],
      "coverImage": "https://picsum.photos/seed/tu-duc/800/500",
      "gallery": [
        "https://picsum.photos/seed/tu-duc-1/800/500",
        "https://picsum.photos/seed/tu-duc-2/800/500",
        "https://picsum.photos/seed/tu-duc-3/800/500"
      ],
      "address": "Thủy Xuân, Thành phố Huế, Thừa Thiên Huế",
      "coordinates": { "lat": 16.4530, "lng": 107.5575 },
      "rating": 4.7,
      "priceRange": "₫",
      "hours": { "open": "07:00", "close": "17:30" },
      "vibe": "Nơi vị hoàng đế thi sĩ dành 16 năm để viết thơ và câu cá.",
      "description": "Lăng Tự Đức là khu vườn ngự uyển tuyệt đẹp mà vua Tự Đức tự thiết kế cho mình khi còn sống. Với hồ Lưu Khiêm, đình Xung Khiêm và rừng thông xanh mát, đây là một trong những lăng tẩm lãng mạn nhất Huế.",
      "insiderTips": [
        "Đây là nơi vua Tự Đức viết hầu hết thơ của ông — mang theo sách để đọc bên hồ.",
        "Ghé thăm vào sáng sớm, sương mù trên hồ tạo ra bầu không khí huyền bí.",
        "Khu nhà bia Khiêm Cung có bài thơ dài nhất của vua — nhớ đọc trước khi vào."
      ],
      "collection": "echoes-of-dynasty"
    },
    {
      "id": "thien-mu-pagoda",
      "name": "Chùa Thiên Mụ",
      "category": "landmark",
      "tags": ["chùa", "tâm linh", "sông Hương"],
      "coverImage": "https://picsum.photos/seed/thien-mu/800/500",
      "gallery": [
        "https://picsum.photos/seed/thienmu-1/800/500",
        "https://picsum.photos/seed/thienmu-2/800/500",
        "https://picsum.photos/seed/thienmu-3/800/500"
      ],
      "address": "Kim Long, Thành phố Huế, Thừa Thiên Huế",
      "coordinates": { "lat": 16.4530, "lng": 107.5468 },
      "rating": 4.6,
      "priceRange": "₫",
      "hours": { "open": "08:00", "close": "17:00" },
      "vibe": "Ngọn tháp 7 tầng soi bóng sông Hương — biểu tượng 400 năm của Huế.",
      "description": "Chùa Thiên Mụ là ngôi chùa cổ nhất và nổi tiếng nhất Huế, được xây dựng năm 1601. Tháp Phước Duyên 7 tầng cao 21m là biểu tượng không thể nhầm lẫn của cố đô, đặc biệt đẹp khi nhìn từ sông Hương.",
      "insiderTips": [
        "Đi thuyền trên sông Hương để chiêm ngưỡng tháp từ mặt nước — khung cảnh tuyệt nhất.",
        "Vào lúc hoàng hôn, chuông chùa ngân vang — dừng lại 10 phút để lắng nghe.",
        "Khu vườn sau chùa có nhiều cây bonsai cổ thụ trăm năm — thường bị khách bỏ qua."
      ],
      "collection": "spiritual-hue"
    },
    {
      "id": "bun-bo-mu-roi",
      "name": "Bún Bò Mụ Rơi",
      "category": "food",
      "tags": ["ẩm thực", "bún bò", "vỉa hè"],
      "coverImage": "https://picsum.photos/seed/bunbo/800/500",
      "gallery": [
        "https://picsum.photos/seed/bunbo-1/800/500",
        "https://picsum.photos/seed/bunbo-2/800/500"
      ],
      "address": "2 Nguyễn Chí Thanh, Phú Hội, Thành phố Huế",
      "coordinates": { "lat": 16.4636, "lng": 107.5910 },
      "rating": 4.5,
      "priceRange": "₫",
      "hours": { "open": "06:00", "close": "10:00" },
      "vibe": "Tô bún bò chuẩn Huế nhất thành phố — chỉ bán buổi sáng.",
      "description": "Quán bún bò gia truyền hơn 30 năm của bà Mụ Rơi. Nước dùng hầm từ xương bò 8 tiếng, sả và mắm ruốc đúng vị Huế gốc. Hết trước 10h sáng mỗi ngày.",
      "insiderTips": [
        "Đến trước 7h30 nếu không muốn hết — quán bán rất nhanh.",
        "Gọi thêm chả cua và huyết — đó là cách người Huế ăn.",
        "Nói với chủ quán 'cho thêm sả' để có hương vị đậm đà hơn."
      ],
      "collection": "local-eats"
    },
    {
      "id": "cong-cafe-hue",
      "name": "Cà phê Cộng Huế",
      "category": "cafe",
      "tags": ["cà phê", "vintage", "sông Hương"],
      "coverImage": "https://picsum.photos/seed/congcafe/800/500",
      "gallery": [
        "https://picsum.photos/seed/congcafe-1/800/500",
        "https://picsum.photos/seed/congcafe-2/800/500"
      ],
      "address": "17 Lê Lợi, Vĩnh Ninh, Thành phố Huế",
      "coordinates": { "lat": 16.4637, "lng": 107.5962 },
      "rating": 4.4,
      "priceRange": "₫₫",
      "hours": { "open": "07:00", "close": "23:00" },
      "vibe": "Phong cách bao cấp Việt Nam, view thẳng ra sông Hương.",
      "description": "Chi nhánh Cộng Cà Phê tại Huế nằm ngay bên bờ sông Hương, với không gian thiết kế theo phong cách hoài cổ Hà Nội thập niên 80. Cà phê dừa là thức uống không thể bỏ qua.",
      "insiderTips": [
        "Gọi cà phê dừa đá — đây là thức uống đặc trưng nhất của Cộng.",
        "Chọn bàn tầng 2 nhìn ra sông để có view đẹp nhất.",
        "Không gian rất ồn vào tối cuối tuần — đến buổi sáng để thư giãn."
      ],
      "collection": "local-eats"
    },
    {
      "id": "thanh-toan-bridge",
      "name": "Cầu Ngói Thanh Toàn",
      "category": "landmark",
      "tags": ["cầu cổ", "kiến trúc", "làng quê"],
      "coverImage": "https://picsum.photos/seed/thanhToan/800/500",
      "gallery": [
        "https://picsum.photos/seed/thanhToan-1/800/500",
        "https://picsum.photos/seed/thanhToan-2/800/500"
      ],
      "address": "Thanh Toàn, Hương Thủy, Thừa Thiên Huế",
      "coordinates": { "lat": 16.4286, "lng": 107.6456 },
      "rating": 4.3,
      "priceRange": "₫",
      "hours": { "open": "07:00", "close": "18:00" },
      "vibe": "Cây cầu ngói 200 năm tuổi ẩn mình trong làng — bí mật của người Huế.",
      "description": "Cầu Ngói Thanh Toàn là cây cầu gỗ có mái che dài 17m, được xây dựng từ thế kỷ 18. Đây là một trong những cây cầu ngói hiếm còn sót lại ở Việt Nam, nằm giữa làng quê yên bình cách trung tâm Huế 8km.",
      "insiderTips": [
        "Thuê xe đạp từ trung tâm thành phố để tận hưởng đường đồng quê — mất khoảng 30 phút.",
        "Phiên chợ họp ngay dưới cầu vào sáng sớm thứ 7 — cảnh tượng rất đẹp.",
        "Đứng giữa cầu nhìn xuống kênh vào lúc hoàng hôn để chụp ảnh đẹp nhất."
      ],
      "collection": "hidden-gems"
    }
  ],
  "collections": [
    {
      "id": "echoes-of-dynasty",
      "title": "Vọng âm triều đại",
      "description": "Những công trình định nghĩa cố đô Huế"
    },
    {
      "id": "spiritual-hue",
      "title": "Tâm linh cố đô",
      "description": "Chùa chiền và đền thờ linh thiêng"
    },
    {
      "id": "local-eats",
      "title": "Ẩm thực vỉa hè",
      "description": "Ăn như người Huế thật sự"
    },
    {
      "id": "hidden-gems",
      "title": "Góc khuất Huế",
      "description": "Những nơi chỉ dân địa phương mới biết"
    }
  ]
}
```

- [ ] **Step 2: Create placeholder for images directory**

```bash
mkdir -p public/images
touch public/images/.gitkeep
```

- [ ] **Step 3: Commit**

```bash
git add src/data/places.json public/images/.gitkeep
git commit -m "feat: add static places data with 7 Hue locations and 4 collections"
```

---

## Task 4: Global design tokens & base styles

**Files:**
- Modify: `src/index.css`

- [ ] **Step 1: Replace index.css with design tokens and base styles**

Replace `src/index.css`:

```css
/* ── Google Fonts ── */
@import url('https://fonts.googleapis.com/css2?family=Noto+Serif:ital,wght@0,400;0,600;0,700;1,400&family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap');

/* ── Design Tokens — The Imperial Chronicler ── */
:root {
  /* Colors */
  --color-primary: #7d0010;
  --color-primary-container: #a01d23;
  --color-secondary: #735c00;
  --color-secondary-fixed: #ffe088;
  --color-tertiary: #5e2b34;
  --color-on-primary: #ffffff;
  --color-on-secondary: #ffffff;
  --color-background: #fdf6ec;
  --color-surface: #fdf6ec;
  --color-surface-container-lowest: #fff8f0;
  --color-surface-container-low: #f5ede0;
  --color-surface-container: #ede0ce;
  --color-surface-container-high: #e5d4be;
  --color-surface-container-highest: #dcc8ac;
  --color-on-surface: #2b1613;
  --color-on-surface-variant: #6b4e3d;
  --color-outline-variant: rgba(107, 78, 61, 0.2);

  /* Typography */
  --font-serif: 'Noto Serif', Georgia, serif;
  --font-sans: 'Plus Jakarta Sans', system-ui, sans-serif;

  /* Spacing */
  --space-xs: 0.25rem;
  --space-sm: 0.5rem;
  --space-md: 1rem;
  --space-lg: 1.5rem;
  --space-xl: 2rem;
  --space-2xl: 3rem;

  /* Radius — "Imperial Arch" */
  --radius-sm: 0.5rem;
  --radius-md: 1rem;
  --radius-lg: 1.5rem;
  --radius-full: 999px;

  /* Shadow */
  --shadow-card: 0px 20px 40px rgba(43, 22, 19, 0.06);
  --shadow-floating: 0px 4px 20px rgba(43, 22, 19, 0.12);

  /* Bottom nav height — used to pad page content */
  --bottom-nav-height: 4rem;
}

/* ── Reset & Base ── */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: 16px;
  -webkit-text-size-adjust: 100%;
}

body {
  font-family: var(--font-sans);
  background-color: var(--color-background);
  color: var(--color-on-surface);
  line-height: 1.6;
  min-height: 100dvh;
  /* Prevent horizontal scroll on mobile */
  overflow-x: hidden;
}

img {
  display: block;
  max-width: 100%;
}

button {
  font-family: var(--font-sans);
  cursor: pointer;
  border: none;
  background: none;
}

a {
  color: inherit;
  text-decoration: none;
}

/* ── App Shell ── */
#root {
  max-width: 480px;
  margin: 0 auto;
  min-height: 100dvh;
  position: relative;
}
```

- [ ] **Step 2: Verify fonts load in browser**

```bash
npm run dev
```

Open browser, inspect element — body should use Plus Jakarta Sans.

- [ ] **Step 3: Commit**

```bash
git add src/index.css
git commit -m "feat: add design tokens and base styles for Imperial Chronicler theme"
```

---

## Task 5: Routing shell & AppLayout

**Files:**
- Modify: `src/App.tsx`
- Create: `src/components/AppLayout.tsx`
- Create: `src/pages/HomePage.tsx` (stub)
- Create: `src/pages/HeritagePage.tsx` (stub)
- Create: `src/pages/MyTripPage.tsx` (stub)
- Create: `src/pages/DetailsPage.tsx` (stub)

- [ ] **Step 1: Create AppLayout component**

Create `src/components/AppLayout.tsx`:

```tsx
import { Outlet } from 'react-router-dom'

export function AppLayout() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', minHeight: '100dvh' }}>
      <main style={{ flex: 1, paddingBottom: 'var(--bottom-nav-height)' }}>
        <Outlet />
      </main>
      {/* BottomNav will be added in Task 6 */}
    </div>
  )
}
```

- [ ] **Step 2: Create stub page components**

Create `src/pages/HomePage.tsx`:
```tsx
export function HomePage() {
  return <div style={{ padding: '1rem' }}>Trang chủ</div>
}
```

Create `src/pages/HeritagePage.tsx`:
```tsx
export function HeritagePage() {
  return <div style={{ padding: '1rem' }}>Di sản</div>
}
```

Create `src/pages/MyTripPage.tsx`:
```tsx
export function MyTripPage() {
  return <div style={{ padding: '1rem' }}>Hành trình</div>
}
```

Create `src/pages/DetailsPage.tsx`:
```tsx
export function DetailsPage() {
  return <div style={{ padding: '1rem' }}>Chi tiết</div>
}
```

- [ ] **Step 3: Wire up routing in App.tsx**

Replace `src/App.tsx`:

```tsx
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom'
import { AppLayout } from './components/AppLayout'
import { HomePage } from './pages/HomePage'
import { HeritagePage } from './pages/HeritagePage'
import { MyTripPage } from './pages/MyTripPage'
import { DetailsPage } from './pages/DetailsPage'

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route element={<AppLayout />}>
          <Route index element={<HomePage />} />
          <Route path="heritage" element={<HeritagePage />} />
          <Route path="my-trip" element={<MyTripPage />} />
        </Route>
        <Route path="details/:id" element={<DetailsPage />} />
        <Route path="heritage/:id" element={<DetailsPage />} />
        <Route path="*" element={<Navigate to="/" replace />} />
      </Routes>
    </BrowserRouter>
  )
}

export default App
```

- [ ] **Step 4: Verify routing works**

```bash
npm run dev
```

Visit `http://localhost:5173/` → "Trang chủ"  
Visit `http://localhost:5173/heritage` → "Di sản"  
Visit `http://localhost:5173/my-trip` → "Hành trình"  
Visit `http://localhost:5173/details/test` → "Chi tiết"

- [ ] **Step 5: Commit**

```bash
git add src/App.tsx src/components/AppLayout.tsx src/pages/
git commit -m "feat: add React Router v6 routing shell with stub pages"
```

---

## Task 6: BottomNav component

**Files:**
- Create: `src/components/BottomNav.tsx`
- Create: `src/components/BottomNav.css`
- Modify: `src/components/AppLayout.tsx`

- [ ] **Step 1: Create BottomNav CSS**

Create `src/components/BottomNav.css`:

```css
.bottom-nav {
  position: fixed;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 100%;
  max-width: 480px;
  height: var(--bottom-nav-height);
  display: flex;
  align-items: stretch;
  background: rgba(253, 246, 236, 0.72);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-top: 1px solid var(--color-outline-variant);
  z-index: 100;
}

.bottom-nav__tab {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 0.2rem;
  color: var(--color-on-surface-variant);
  transition: color 0.2s;
  -webkit-tap-highlight-color: transparent;
}

.bottom-nav__tab--active {
  color: var(--color-primary);
}

.bottom-nav__icon {
  font-size: 1.3rem;
  line-height: 1;
}

.bottom-nav__label {
  font-family: var(--font-sans);
  font-size: 0.65rem;
  font-weight: 600;
  letter-spacing: 0.03em;
}
```

- [ ] **Step 2: Create BottomNav component**

Create `src/components/BottomNav.tsx`:

```tsx
import { NavLink } from 'react-router-dom'
import './BottomNav.css'

const TABS = [
  { to: '/', label: 'Trang chủ', icon: '🏠' },
  { to: '/heritage', label: 'Di sản', icon: '🏛' },
  { to: '/my-trip', label: 'Hành trình', icon: '🎒' },
] as const

export function BottomNav() {
  return (
    <nav className="bottom-nav" aria-label="Điều hướng chính">
      {TABS.map(tab => (
        <NavLink
          key={tab.to}
          to={tab.to}
          end={tab.to === '/'}
          className={({ isActive }) =>
            `bottom-nav__tab${isActive ? ' bottom-nav__tab--active' : ''}`
          }
        >
          <span className="bottom-nav__icon" aria-hidden="true">{tab.icon}</span>
          <span className="bottom-nav__label">{tab.label}</span>
        </NavLink>
      ))}
    </nav>
  )
}
```

- [ ] **Step 3: Add BottomNav to AppLayout**

Replace `src/components/AppLayout.tsx`:

```tsx
import { Outlet } from 'react-router-dom'
import { BottomNav } from './BottomNav'

export function AppLayout() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', minHeight: '100dvh' }}>
      <main style={{ flex: 1, paddingBottom: 'var(--bottom-nav-height)' }}>
        <Outlet />
      </main>
      <BottomNav />
    </div>
  )
}
```

- [ ] **Step 4: Verify bottom nav renders and active state works**

```bash
npm run dev
```

- Bottom nav appears at the bottom on all 3 tab routes
- Active tab is highlighted in Imperial Red (`#7d0010`)
- Details page (`/details/test`) has NO bottom nav

- [ ] **Step 5: Commit**

```bash
git add src/components/BottomNav.tsx src/components/BottomNav.css src/components/AppLayout.tsx
git commit -m "feat: add glassmorphism BottomNav with 3 tabs"
```

---

## Task 7: useMyTrip hook with tests

**Files:**
- Create: `src/hooks/useMyTrip.ts`
- Create: `src/hooks/useMyTrip.test.ts`

- [ ] **Step 1: Write failing tests first**

Create `src/hooks/useMyTrip.test.ts`:

```ts
import { renderHook, act } from '@testing-library/react'
import { describe, it, expect, beforeEach } from 'vitest'
import { useMyTrip } from './useMyTrip'

beforeEach(() => {
  localStorage.clear()
})

describe('useMyTrip', () => {
  it('starts with empty list when localStorage is empty', () => {
    const { result } = renderHook(() => useMyTrip())
    expect(result.current.savedIds).toEqual([])
  })

  it('restores saved ids from localStorage on mount', () => {
    localStorage.setItem('hue-ni-trip', JSON.stringify(['tu-duc-tomb', 'hue-citadel']))
    const { result } = renderHook(() => useMyTrip())
    expect(result.current.savedIds).toEqual(['tu-duc-tomb', 'hue-citadel'])
  })

  it('adds a place id', () => {
    const { result } = renderHook(() => useMyTrip())
    act(() => result.current.addPlace('khai-dinh-tomb'))
    expect(result.current.savedIds).toContain('khai-dinh-tomb')
  })

  it('does not add duplicate ids', () => {
    const { result } = renderHook(() => useMyTrip())
    act(() => {
      result.current.addPlace('khai-dinh-tomb')
      result.current.addPlace('khai-dinh-tomb')
    })
    expect(result.current.savedIds).toHaveLength(1)
  })

  it('removes a place id', () => {
    const { result } = renderHook(() => useMyTrip())
    act(() => result.current.addPlace('khai-dinh-tomb'))
    act(() => result.current.removePlace('khai-dinh-tomb'))
    expect(result.current.savedIds).not.toContain('khai-dinh-tomb')
  })

  it('removes only the specified id', () => {
    const { result } = renderHook(() => useMyTrip())
    act(() => {
      result.current.addPlace('khai-dinh-tomb')
      result.current.addPlace('tu-duc-tomb')
    })
    act(() => result.current.removePlace('khai-dinh-tomb'))
    expect(result.current.savedIds).toContain('tu-duc-tomb')
    expect(result.current.savedIds).not.toContain('khai-dinh-tomb')
  })

  it('clears all saved ids', () => {
    const { result } = renderHook(() => useMyTrip())
    act(() => {
      result.current.addPlace('khai-dinh-tomb')
      result.current.addPlace('tu-duc-tomb')
    })
    act(() => result.current.clearAll())
    expect(result.current.savedIds).toEqual([])
  })

  it('isSaved returns true for a saved id', () => {
    const { result } = renderHook(() => useMyTrip())
    act(() => result.current.addPlace('khai-dinh-tomb'))
    expect(result.current.isSaved('khai-dinh-tomb')).toBe(true)
  })

  it('isSaved returns false for an unsaved id', () => {
    const { result } = renderHook(() => useMyTrip())
    expect(result.current.isSaved('khai-dinh-tomb')).toBe(false)
  })

  it('persists changes to localStorage', () => {
    const { result } = renderHook(() => useMyTrip())
    act(() => result.current.addPlace('khai-dinh-tomb'))
    expect(JSON.parse(localStorage.getItem('hue-ni-trip')!)).toContain('khai-dinh-tomb')
  })

  it('clears localStorage on clearAll', () => {
    const { result } = renderHook(() => useMyTrip())
    act(() => result.current.addPlace('khai-dinh-tomb'))
    act(() => result.current.clearAll())
    expect(JSON.parse(localStorage.getItem('hue-ni-trip')!)).toEqual([])
  })
})
```

- [ ] **Step 2: Run tests — verify they all fail**

```bash
npm test
```

Expected: all 11 tests fail with "Cannot find module './useMyTrip'"

- [ ] **Step 3: Implement the hook**

Create `src/hooks/useMyTrip.ts`:

```ts
import { useState } from 'react'

const STORAGE_KEY = 'hue-ni-trip'

function getStoredIds(): string[] {
  try {
    return JSON.parse(localStorage.getItem(STORAGE_KEY) ?? '[]')
  } catch {
    return []
  }
}

export function useMyTrip() {
  const [savedIds, setSavedIds] = useState<string[]>(getStoredIds)

  function persist(ids: string[]) {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(ids))
    setSavedIds(ids)
  }

  function addPlace(id: string) {
    setSavedIds(current => {
      if (current.includes(id)) return current
      const next = [...current, id]
      localStorage.setItem(STORAGE_KEY, JSON.stringify(next))
      return next
    })
  }

  function removePlace(id: string) {
    setSavedIds(current => {
      const next = current.filter(sid => sid !== id)
      localStorage.setItem(STORAGE_KEY, JSON.stringify(next))
      return next
    })
  }

  function clearAll() {
    persist([])
  }

  function isSaved(id: string): boolean {
    return savedIds.includes(id)
  }

  return { savedIds, addPlace, removePlace, clearAll, isSaved }
}
```

- [ ] **Step 4: Run tests — verify they all pass**

```bash
npm test
```

Expected: 11 tests pass, 0 fail.

- [ ] **Step 5: Commit**

```bash
git add src/hooks/useMyTrip.ts src/hooks/useMyTrip.test.ts
git commit -m "feat: add useMyTrip hook with localStorage persistence (11 tests)"
```

---

## Task 8: PlaceCard component

**Files:**
- Create: `src/components/PlaceCard.tsx`
- Create: `src/components/PlaceCard.css`

- [ ] **Step 1: Create PlaceCard CSS**

Create `src/components/PlaceCard.css`:

```css
/* ── Vertical card (Heritage list) ── */
.place-card {
  background: var(--color-surface-container-lowest);
  border-radius: var(--radius-lg);
  overflow: hidden;
  box-shadow: var(--shadow-card);
  cursor: pointer;
  -webkit-tap-highlight-color: transparent;
  transition: transform 0.15s ease, box-shadow 0.15s ease;
}

.place-card:active {
  transform: scale(0.98);
}

.place-card__image-wrap {
  position: relative;
  width: 100%;
  aspect-ratio: 16 / 9;
  overflow: hidden;
}

.place-card__image-wrap img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.place-card__category-badge {
  position: absolute;
  top: var(--space-sm);
  left: var(--space-sm);
  background: var(--color-tertiary);
  color: #fff;
  font-family: var(--font-sans);
  font-size: 0.6rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  padding: 0.2rem 0.5rem;
  border-radius: var(--radius-full);
}

.place-card__body {
  padding: var(--space-md);
}

.place-card__name {
  font-family: var(--font-serif);
  font-size: 1.05rem;
  font-weight: 700;
  color: var(--color-on-surface);
  margin-bottom: var(--space-xs);
}

.place-card__vibe {
  font-size: 0.8rem;
  color: var(--color-on-surface-variant);
  margin-bottom: var(--space-sm);
  line-height: 1.4;
}

.place-card__meta {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
  margin-bottom: var(--space-xs);
}

.place-card__rating {
  font-size: 0.8rem;
  font-weight: 600;
  color: var(--color-secondary);
}

.place-card__price {
  font-size: 0.8rem;
  color: var(--color-on-surface-variant);
}

.place-card__address {
  font-size: 0.75rem;
  color: var(--color-on-surface-variant);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* ── Horizontal card (Home "Near Me") ── */
.place-card--horizontal {
  display: flex;
  flex-direction: column;
  width: 200px;
  flex-shrink: 0;
}

.place-card--horizontal .place-card__image-wrap {
  aspect-ratio: 4 / 3;
}
```

- [ ] **Step 2: Create PlaceCard component**

Create `src/components/PlaceCard.tsx`:

```tsx
import { useNavigate } from 'react-router-dom'
import type { Place } from '../types'
import './PlaceCard.css'

const CATEGORY_LABELS: Record<string, string> = {
  tomb: 'Lăng tẩm',
  landmark: 'Di tích',
  cafe: 'Cà phê',
  food: 'Ẩm thực',
  homestay: 'Homestay',
  service: 'Dịch vụ',
}

interface PlaceCardProps {
  place: Place
  variant?: 'vertical' | 'horizontal'
}

export function PlaceCard({ place, variant = 'vertical' }: PlaceCardProps) {
  const navigate = useNavigate()

  function handleClick() {
    navigate(`/details/${place.id}`)
  }

  return (
    <article
      className={`place-card${variant === 'horizontal' ? ' place-card--horizontal' : ''}`}
      onClick={handleClick}
      role="button"
      tabIndex={0}
      aria-label={place.name}
      onKeyDown={e => e.key === 'Enter' && handleClick()}
    >
      <div className="place-card__image-wrap">
        <img src={place.coverImage} alt={place.name} loading="lazy" />
        <span className="place-card__category-badge">
          {CATEGORY_LABELS[place.category] ?? place.category}
        </span>
      </div>
      <div className="place-card__body">
        <h3 className="place-card__name">{place.name}</h3>
        <p className="place-card__vibe">{place.vibe}</p>
        <div className="place-card__meta">
          <span className="place-card__rating">★ {place.rating}</span>
          <span className="place-card__price">{place.priceRange}</span>
        </div>
        <p className="place-card__address">{place.address}</p>
      </div>
    </article>
  )
}
```

- [ ] **Step 3: Verify TypeScript compiles**

```bash
npm run build
```

Expected: no type errors.

- [ ] **Step 4: Commit**

```bash
git add src/components/PlaceCard.tsx src/components/PlaceCard.css
git commit -m "feat: add shared PlaceCard component (vertical + horizontal variants)"
```

---

## Task 9: CategoryFilter component

**Files:**
- Create: `src/components/CategoryFilter.tsx`
- Create: `src/components/CategoryFilter.css`

- [ ] **Step 1: Create CategoryFilter CSS**

Create `src/components/CategoryFilter.css`:

```css
.category-filter {
  display: flex;
  gap: var(--space-sm);
  overflow-x: auto;
  padding: var(--space-sm) var(--space-md);
  scrollbar-width: none;
  -ms-overflow-style: none;
}

.category-filter::-webkit-scrollbar {
  display: none;
}

.category-filter__chip {
  flex-shrink: 0;
  padding: 0.4rem 1rem;
  border-radius: var(--radius-full);
  background: var(--color-surface-container-low);
  color: var(--color-on-surface-variant);
  font-family: var(--font-sans);
  font-size: 0.8rem;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.15s, color 0.15s;
  -webkit-tap-highlight-color: transparent;
}

.category-filter__chip--active {
  background: var(--color-primary);
  color: var(--color-on-primary);
}
```

- [ ] **Step 2: Create CategoryFilter component**

Create `src/components/CategoryFilter.tsx`:

```tsx
import './CategoryFilter.css'

export interface FilterOption {
  value: string
  label: string
}

interface CategoryFilterProps {
  options: FilterOption[]
  selected: string
  onChange: (value: string) => void
}

export function CategoryFilter({ options, selected, onChange }: CategoryFilterProps) {
  return (
    <div className="category-filter" role="group" aria-label="Lọc theo danh mục">
      {options.map(opt => (
        <button
          key={opt.value}
          className={`category-filter__chip${selected === opt.value ? ' category-filter__chip--active' : ''}`}
          onClick={() => onChange(opt.value)}
          aria-pressed={selected === opt.value}
        >
          {opt.label}
        </button>
      ))}
    </div>
  )
}
```

- [ ] **Step 3: Commit**

```bash
git add src/components/CategoryFilter.tsx src/components/CategoryFilter.css
git commit -m "feat: add horizontally scrollable CategoryFilter chip component"
```

---

## Task 10: Heritage page

**Files:**
- Modify: `src/pages/HeritagePage.tsx`
- Create: `src/pages/HeritagePage.css`

- [ ] **Step 1: Create HeritagePage CSS**

Create `src/pages/HeritagePage.css`:

```css
.heritage-page {
  min-height: 100%;
}

.heritage-page__header {
  padding: var(--space-lg) var(--space-md) var(--space-sm);
}

.heritage-page__title {
  font-family: var(--font-serif);
  font-size: 1.6rem;
  font-weight: 700;
  color: var(--color-on-surface);
  margin-bottom: var(--space-xs);
}

.heritage-page__subtitle {
  font-size: 0.85rem;
  color: var(--color-on-surface-variant);
}

.heritage-page__list {
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
  padding: var(--space-md);
}
```

- [ ] **Step 2: Implement HeritagePage**

Replace `src/pages/HeritagePage.tsx`:

```tsx
import { useState } from 'react'
import { CategoryFilter, type FilterOption } from '../components/CategoryFilter'
import { PlaceCard } from '../components/PlaceCard'
import placesData from '../data/places.json'
import type { Place } from '../types'
import './HeritagePage.css'

const FILTER_OPTIONS: FilterOption[] = [
  { value: 'all', label: 'Tất cả' },
  { value: 'tomb', label: 'Lăng tẩm' },
  { value: 'landmark', label: 'Di tích' },
  { value: 'cafe', label: 'Cà phê' },
  { value: 'food', label: 'Ẩm thực' },
  { value: 'homestay', label: 'Homestay' },
  { value: 'service', label: 'Dịch vụ' },
]

const ALL_PLACES = placesData.places as Place[]

export function HeritagePage() {
  const [selectedCategory, setSelectedCategory] = useState('all')

  const filtered = selectedCategory === 'all'
    ? ALL_PLACES
    : ALL_PLACES.filter(p => p.category === selectedCategory)

  return (
    <div className="heritage-page">
      <header className="heritage-page__header">
        <h1 className="heritage-page__title">Vọng âm triều đại</h1>
        <p className="heritage-page__subtitle">
          {filtered.length} địa điểm
        </p>
      </header>

      <CategoryFilter
        options={FILTER_OPTIONS}
        selected={selectedCategory}
        onChange={setSelectedCategory}
      />

      <div className="heritage-page__list">
        {filtered.map(place => (
          <PlaceCard key={place.id} place={place} variant="vertical" />
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Verify in browser**

```bash
npm run dev
```

Visit `/heritage`:
- Header shows "Vọng âm triều đại"
- All 7 places listed as vertical cards
- Tapping "Lăng tẩm" filter shows only tomb places
- Tapping a card navigates to `/details/<id>`

- [ ] **Step 4: Commit**

```bash
git add src/pages/HeritagePage.tsx src/pages/HeritagePage.css
git commit -m "feat: implement Heritage page with category filter and place list"
```

---

## Task 11: Home page

**Files:**
- Modify: `src/pages/HomePage.tsx`
- Create: `src/pages/HomePage.css`

- [ ] **Step 1: Create HomePage CSS**

Create `src/pages/HomePage.css`:

```css
.home-page__header {
  background: var(--color-primary);
  padding: var(--space-lg) var(--space-md) var(--space-md);
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.home-page__app-name {
  font-family: var(--font-serif);
  font-size: 1.2rem;
  font-weight: 700;
  color: var(--color-on-primary);
  letter-spacing: -0.01em;
}

.home-page__search-wrap {
  padding: var(--space-md) var(--space-md) 0;
}

.home-page__search {
  width: 100%;
  padding: 0.75rem var(--space-md);
  background: var(--color-surface-container-highest);
  border: none;
  border-radius: var(--radius-full);
  font-family: var(--font-sans);
  font-size: 0.9rem;
  color: var(--color-on-surface);
  outline: none;
}

.home-page__search::placeholder {
  color: var(--color-on-surface-variant);
}

.home-page__section {
  margin-top: var(--space-lg);
}

.home-page__section-header {
  padding: 0 var(--space-md) var(--space-sm);
  display: flex;
  align-items: baseline;
  gap: var(--space-sm);
}

.home-page__section-title {
  font-family: var(--font-serif);
  font-size: 1.1rem;
  font-weight: 700;
  color: var(--color-on-surface);
}

.home-page__section-subtitle {
  font-size: 0.75rem;
  color: var(--color-on-surface-variant);
}

/* Horizontal scroll row */
.home-page__scroll-row {
  display: flex;
  gap: var(--space-md);
  overflow-x: auto;
  padding: 0 var(--space-md) var(--space-md);
  scrollbar-width: none;
  -ms-overflow-style: none;
}

.home-page__scroll-row::-webkit-scrollbar {
  display: none;
}

/* Collections */
.home-page__collection {
  margin-top: var(--space-xl);
}

.home-page__collection-banner {
  margin: 0 var(--space-md) var(--space-md);
  background: var(--color-surface-container);
  border-radius: var(--radius-lg);
  padding: var(--space-md) var(--space-lg);
}

.home-page__collection-title {
  font-family: var(--font-serif);
  font-size: 1rem;
  font-weight: 700;
  color: var(--color-on-surface);
  margin-bottom: var(--space-xs);
}

.home-page__collection-desc {
  font-size: 0.8rem;
  color: var(--color-on-surface-variant);
}

.home-page__empty {
  padding: var(--space-xl) var(--space-md);
  text-align: center;
  color: var(--color-on-surface-variant);
  font-size: 0.9rem;
}
```

- [ ] **Step 2: Implement HomePage**

Replace `src/pages/HomePage.tsx`:

```tsx
import { useState } from 'react'
import { CategoryFilter, type FilterOption } from '../components/CategoryFilter'
import { PlaceCard } from '../components/PlaceCard'
import placesData from '../data/places.json'
import type { Place, Collection } from '../types'
import './HomePage.css'

const FILTER_OPTIONS: FilterOption[] = [
  { value: 'all', label: 'Tất cả' },
  { value: 'tomb', label: 'Lăng tẩm' },
  { value: 'landmark', label: 'Di tích' },
  { value: 'cafe', label: 'Cà phê' },
  { value: 'food', label: 'Ẩm thực' },
  { value: 'homestay', label: 'Homestay' },
  { value: 'service', label: 'Dịch vụ' },
]

const ALL_PLACES = placesData.places as Place[]
const ALL_COLLECTIONS = placesData.collections as Collection[]

export function HomePage() {
  const [query, setQuery] = useState('')
  const [selectedCategory, setSelectedCategory] = useState('all')

  const searchResults = query.trim().length > 0
    ? ALL_PLACES.filter(p =>
        p.name.toLowerCase().includes(query.toLowerCase()) ||
        p.tags.some(t => t.toLowerCase().includes(query.toLowerCase()))
      )
    : null

  const nearMePlaces = ALL_PLACES.slice(0, 5)

  function getCollectionPlaces(collectionId: string): Place[] {
    return ALL_PLACES.filter(p => p.collection === collectionId)
  }

  return (
    <div className="home-page">
      <header className="home-page__header">
        <span className="home-page__app-name">The Imperial Chronicler</span>
      </header>

      {/* Search */}
      <div className="home-page__search-wrap">
        <input
          className="home-page__search"
          type="search"
          placeholder="Tìm địa điểm, ẩm thực..."
          value={query}
          onChange={e => setQuery(e.target.value)}
          aria-label="Tìm kiếm địa điểm"
        />
      </div>

      {/* Search results */}
      {searchResults !== null && (
        <div className="home-page__section">
          <div className="home-page__section-header">
            <h2 className="home-page__section-title">Kết quả tìm kiếm</h2>
            <span className="home-page__section-subtitle">{searchResults.length} nơi</span>
          </div>
          {searchResults.length === 0 ? (
            <p className="home-page__empty">Không tìm thấy địa điểm phù hợp.</p>
          ) : (
            <div className="home-page__scroll-row">
              {searchResults.map(place => (
                <PlaceCard key={place.id} place={place} variant="horizontal" />
              ))}
            </div>
          )}
        </div>
      )}

      {/* Category filter + Near Me (shown when not searching) */}
      {searchResults === null && (
        <>
          <div className="home-page__section">
            <CategoryFilter
              options={FILTER_OPTIONS}
              selected={selectedCategory}
              onChange={setSelectedCategory}
            />
          </div>

          <div className="home-page__section">
            <div className="home-page__section-header">
              <h2 className="home-page__section-title">Gần đây</h2>
              <span className="home-page__section-subtitle">Những nơi đáng ghé</span>
            </div>
            <div className="home-page__scroll-row">
              {(selectedCategory === 'all'
                ? nearMePlaces
                : ALL_PLACES.filter(p => p.category === selectedCategory).slice(0, 5)
              ).map(place => (
                <PlaceCard key={place.id} place={place} variant="horizontal" />
              ))}
            </div>
          </div>

          {/* Curated Collections */}
          {ALL_COLLECTIONS.map(collection => {
            const places = getCollectionPlaces(collection.id)
            if (places.length === 0) return null
            return (
              <div key={collection.id} className="home-page__collection">
                <div className="home-page__collection-banner">
                  <h2 className="home-page__collection-title">{collection.title}</h2>
                  <p className="home-page__collection-desc">{collection.description}</p>
                </div>
                <div className="home-page__scroll-row">
                  {places.map(place => (
                    <PlaceCard key={place.id} place={place} variant="horizontal" />
                  ))}
                </div>
              </div>
            )
          })}
        </>
      )}
    </div>
  )
}
```

- [ ] **Step 3: Verify in browser**

```bash
npm run dev
```

Visit `/`:
- Imperial Red header with app name
- Search bar — typing filters places in real time
- "Gần đây" section: 5 horizontal scrollable cards
- Curated Collections: 4 collection banners each with horizontal card rows
- Category filter changes "Gần đây" cards

- [ ] **Step 4: Commit**

```bash
git add src/pages/HomePage.tsx src/pages/HomePage.css
git commit -m "feat: implement Home page with search, filters, Near Me, and collections"
```

---

## Task 12: ImageGallery component

**Files:**
- Create: `src/components/ImageGallery.tsx`
- Create: `src/components/ImageGallery.css`

- [ ] **Step 1: Create ImageGallery CSS**

Create `src/components/ImageGallery.css`:

```css
.image-gallery {
  display: flex;
  overflow-x: auto;
  scroll-snap-type: x mandatory;
  gap: var(--space-sm);
  padding: 0 var(--space-md) var(--space-md);
  scrollbar-width: none;
  -ms-overflow-style: none;
}

.image-gallery::-webkit-scrollbar {
  display: none;
}

.image-gallery__item {
  flex-shrink: 0;
  width: 75vw;
  max-width: 320px;
  aspect-ratio: 4 / 3;
  object-fit: cover;
  border-radius: var(--radius-md);
  scroll-snap-align: start;
}
```

- [ ] **Step 2: Create ImageGallery component**

Create `src/components/ImageGallery.tsx`:

```tsx
import './ImageGallery.css'

interface ImageGalleryProps {
  images: string[]
  placeName: string
}

export function ImageGallery({ images, placeName }: ImageGalleryProps) {
  if (images.length === 0) return null

  return (
    <div className="image-gallery" aria-label={`Ảnh của ${placeName}`}>
      {images.map((src, i) => (
        <img
          key={i}
          src={src}
          alt={`${placeName} — ảnh ${i + 1}`}
          className="image-gallery__item"
          loading="lazy"
        />
      ))}
    </div>
  )
}
```

- [ ] **Step 3: Commit**

```bash
git add src/components/ImageGallery.tsx src/components/ImageGallery.css
git commit -m "feat: add swipeable ImageGallery with CSS scroll-snap"
```

---

## Task 13: Details page

**Files:**
- Modify: `src/pages/DetailsPage.tsx`
- Create: `src/pages/DetailsPage.css`

- [ ] **Step 1: Create DetailsPage CSS**

Create `src/pages/DetailsPage.css`:

```css
.details-page {
  min-height: 100dvh;
  background: var(--color-surface);
  padding-bottom: 5rem;
}

/* Back button */
.details-page__back {
  position: absolute;
  top: var(--space-md);
  left: var(--space-md);
  z-index: 10;
  width: 2.5rem;
  height: 2.5rem;
  border-radius: var(--radius-full);
  background: rgba(253, 246, 236, 0.85);
  backdrop-filter: blur(8px);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.2rem;
  cursor: pointer;
  box-shadow: var(--shadow-floating);
  border: none;
}

/* Cover image */
.details-page__cover {
  position: relative;
  width: 100%;
  aspect-ratio: 4 / 3;
}

.details-page__cover img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

/* Content */
.details-page__content {
  padding: var(--space-lg) var(--space-md) 0;
}

.details-page__category {
  font-family: var(--font-sans);
  font-size: 0.65rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--color-secondary);
  margin-bottom: var(--space-xs);
}

.details-page__name {
  font-family: var(--font-serif);
  font-size: 1.8rem;
  font-weight: 700;
  color: var(--color-on-surface);
  line-height: 1.2;
  margin-bottom: var(--space-sm);
}

.details-page__vibe {
  font-size: 0.95rem;
  color: var(--color-on-surface-variant);
  font-style: italic;
  margin-bottom: var(--space-lg);
  line-height: 1.5;
}

/* Info grid */
.details-page__info {
  background: var(--color-surface-container-low);
  border-radius: var(--radius-lg);
  padding: var(--space-md);
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-sm);
  margin-bottom: var(--space-lg);
}

.details-page__info-item {
  display: flex;
  flex-direction: column;
  gap: 0.15rem;
}

.details-page__info-label {
  font-size: 0.65rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--color-on-surface-variant);
}

.details-page__info-value {
  font-size: 0.85rem;
  font-weight: 600;
  color: var(--color-on-surface);
}

/* Description */
.details-page__description {
  font-size: 0.9rem;
  color: var(--color-on-surface-variant);
  line-height: 1.7;
  margin-bottom: var(--space-lg);
}

/* Gallery section */
.details-page__gallery-title {
  font-family: var(--font-serif);
  font-size: 1rem;
  font-weight: 700;
  color: var(--color-on-surface);
  padding: 0 var(--space-md) var(--space-sm);
}

/* Insider tips */
.details-page__tips {
  padding: 0 var(--space-md);
  margin-bottom: var(--space-lg);
}

.details-page__tips-title {
  font-family: var(--font-serif);
  font-size: 1rem;
  font-weight: 700;
  color: var(--color-on-surface);
  margin-bottom: var(--space-md);
}

.details-page__tip {
  background: var(--color-secondary-fixed);
  border-radius: var(--radius-md);
  padding: var(--space-md);
  margin-bottom: var(--space-sm);
  font-size: 0.85rem;
  color: var(--color-on-surface);
  line-height: 1.6;
}

/* Sticky action bar */
.details-page__actions {
  position: fixed;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 100%;
  max-width: 480px;
  padding: var(--space-sm) var(--space-md);
  background: rgba(253, 246, 236, 0.92);
  backdrop-filter: blur(20px);
  border-top: 1px solid var(--color-outline-variant);
  display: flex;
  gap: var(--space-sm);
  z-index: 100;
}

.details-page__action-btn {
  flex: 1;
  padding: 0.7rem var(--space-sm);
  border-radius: var(--radius-lg);
  font-family: var(--font-sans);
  font-size: 0.8rem;
  font-weight: 700;
  cursor: pointer;
  border: none;
  -webkit-tap-highlight-color: transparent;
  transition: opacity 0.15s;
}

.details-page__action-btn:active {
  opacity: 0.8;
}

.details-page__action-btn--primary {
  background: linear-gradient(45deg, var(--color-primary), var(--color-primary-container));
  color: var(--color-on-primary);
}

.details-page__action-btn--secondary {
  background: var(--color-surface-container);
  color: var(--color-on-surface);
}

.details-page__not-found {
  padding: var(--space-2xl) var(--space-md);
  text-align: center;
  color: var(--color-on-surface-variant);
}
```

- [ ] **Step 2: Implement DetailsPage**

Replace `src/pages/DetailsPage.tsx`:

```tsx
import { useParams, useNavigate } from 'react-router-dom'
import { ImageGallery } from '../components/ImageGallery'
import { useMyTrip } from '../hooks/useMyTrip'
import placesData from '../data/places.json'
import type { Place } from '../types'
import './DetailsPage.css'

const CATEGORY_LABELS: Record<string, string> = {
  tomb: 'Lăng tẩm',
  landmark: 'Di tích',
  cafe: 'Cà phê',
  food: 'Ẩm thực',
  homestay: 'Homestay',
  service: 'Dịch vụ',
}

const ALL_PLACES = placesData.places as Place[]

export function DetailsPage() {
  const { id } = useParams<{ id: string }>()
  const navigate = useNavigate()
  const { isSaved, addPlace, removePlace } = useMyTrip()

  const place = ALL_PLACES.find(p => p.id === id)

  if (!place) {
    return (
      <div className="details-page">
        <button className="details-page__back" onClick={() => navigate(-1)} aria-label="Quay lại">
          ←
        </button>
        <div className="details-page__not-found">
          <p>Không tìm thấy địa điểm này.</p>
        </div>
      </div>
    )
  }

  const saved = isSaved(place.id)

  function handleDirections() {
    const url = `https://www.google.com/maps/dir/?api=1&destination=${place.coordinates.lat},${place.coordinates.lng}`
    window.open(url, '_blank', 'noopener,noreferrer')
  }

  function handleSave() {
    if (saved) {
      removePlace(place.id)
    } else {
      addPlace(place.id)
    }
  }

  async function handleShare() {
    if (navigator.share) {
      await navigator.share({
        title: place.name,
        text: place.vibe,
        url: window.location.href,
      })
    } else {
      await navigator.clipboard.writeText(window.location.href)
    }
  }

  return (
    <div className="details-page">
      <button
        className="details-page__back"
        onClick={() => navigate(-1)}
        aria-label="Quay lại"
      >
        ←
      </button>

      {/* Cover image */}
      <div className="details-page__cover">
        <img src={place.coverImage} alt={place.name} />
      </div>

      {/* Main content */}
      <div className="details-page__content">
        <p className="details-page__category">
          {CATEGORY_LABELS[place.category] ?? place.category}
        </p>
        <h1 className="details-page__name">{place.name}</h1>
        <p className="details-page__vibe">{place.vibe}</p>

        {/* Info grid */}
        <div className="details-page__info">
          <div className="details-page__info-item">
            <span className="details-page__info-label">Đánh giá</span>
            <span className="details-page__info-value">★ {place.rating}</span>
          </div>
          <div className="details-page__info-item">
            <span className="details-page__info-label">Giá</span>
            <span className="details-page__info-value">{place.priceRange}</span>
          </div>
          <div className="details-page__info-item">
            <span className="details-page__info-label">Giờ mở cửa</span>
            <span className="details-page__info-value">
              {place.hours.open} – {place.hours.close}
            </span>
          </div>
          <div className="details-page__info-item">
            <span className="details-page__info-label">Địa chỉ</span>
            <span className="details-page__info-value" style={{ fontSize: '0.75rem' }}>
              {place.address}
            </span>
          </div>
        </div>

        <p className="details-page__description">{place.description}</p>
      </div>

      {/* Gallery */}
      {place.gallery.length > 0 && (
        <>
          <h2 className="details-page__gallery-title">Hình ảnh</h2>
          <ImageGallery images={place.gallery} placeName={place.name} />
        </>
      )}

      {/* Insider tips */}
      {place.insiderTips.length > 0 && (
        <div className="details-page__tips">
          <h2 className="details-page__tips-title">💡 Mẹo nội bộ</h2>
          {place.insiderTips.map((tip, i) => (
            <div key={i} className="details-page__tip">
              {tip}
            </div>
          ))}
        </div>
      )}

      {/* Sticky action bar */}
      <div className="details-page__actions">
        <button
          className="details-page__action-btn details-page__action-btn--primary"
          onClick={handleDirections}
          aria-label="Chỉ đường"
        >
          🧭 Đường đi
        </button>
        <button
          className="details-page__action-btn details-page__action-btn--secondary"
          onClick={handleSave}
          aria-label={saved ? 'Bỏ lưu' : 'Lưu địa điểm'}
        >
          {saved ? '📌 Đã lưu' : '📌 Lưu'}
        </button>
        <button
          className="details-page__action-btn details-page__action-btn--secondary"
          onClick={handleShare}
          aria-label="Chia sẻ"
        >
          🔗 Chia sẻ
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Verify in browser**

```bash
npm run dev
```

From Heritage or Home, tap a place card:
- Full-screen with cover image + back arrow
- No bottom nav visible
- Info grid shows rating, price, hours, address
- Gallery swipeable horizontally
- Insider Tips shown in gold cards
- Sticky bar: "Đường đi" opens Google Maps, "Lưu" toggles save state, "Chia sẻ" triggers Web Share

- [ ] **Step 4: Commit**

```bash
git add src/pages/DetailsPage.tsx src/pages/DetailsPage.css
git commit -m "feat: implement full-screen Details page with gallery, tips, and action bar"
```

---

## Task 14: OnboardingModal

**Files:**
- Create: `src/components/OnboardingModal.tsx`
- Create: `src/components/OnboardingModal.css`
- Modify: `src/pages/HomePage.tsx`

- [ ] **Step 1: Create OnboardingModal CSS**

Create `src/components/OnboardingModal.css`:

```css
.onboarding-overlay {
  position: fixed;
  inset: 0;
  background: rgba(43, 22, 19, 0.6);
  backdrop-filter: blur(4px);
  z-index: 200;
  display: flex;
  align-items: flex-end;
  justify-content: center;
}

.onboarding-modal {
  width: 100%;
  max-width: 480px;
  background: var(--color-surface-container-lowest);
  border-radius: var(--radius-lg) var(--radius-lg) 0 0;
  padding: var(--space-xl) var(--space-lg);
  text-align: center;
}

.onboarding-modal__emoji {
  font-size: 3rem;
  margin-bottom: var(--space-md);
}

.onboarding-modal__title {
  font-family: var(--font-serif);
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--color-on-surface);
  margin-bottom: var(--space-sm);
}

.onboarding-modal__subtitle {
  font-size: 0.9rem;
  color: var(--color-on-surface-variant);
  line-height: 1.6;
  margin-bottom: var(--space-xl);
}

.onboarding-modal__btn-primary {
  width: 100%;
  padding: 0.9rem;
  background: linear-gradient(45deg, var(--color-primary), var(--color-primary-container));
  color: var(--color-on-primary);
  border: none;
  border-radius: var(--radius-lg);
  font-family: var(--font-sans);
  font-size: 0.95rem;
  font-weight: 700;
  cursor: pointer;
  margin-bottom: var(--space-sm);
  -webkit-tap-highlight-color: transparent;
}

.onboarding-modal__btn-skip {
  width: 100%;
  padding: 0.7rem;
  background: none;
  border: none;
  font-family: var(--font-sans);
  font-size: 0.85rem;
  color: var(--color-on-surface-variant);
  cursor: pointer;
  -webkit-tap-highlight-color: transparent;
}
```

- [ ] **Step 2: Create OnboardingModal component**

Create `src/components/OnboardingModal.tsx`:

```tsx
import './OnboardingModal.css'

const ONBOARDING_KEY = 'hue-ni-onboarded'

export function hasSeenOnboarding(): boolean {
  return localStorage.getItem(ONBOARDING_KEY) === 'true'
}

interface OnboardingModalProps {
  onDismiss: () => void
}

export function OnboardingModal({ onDismiss }: OnboardingModalProps) {
  function dismiss() {
    localStorage.setItem(ONBOARDING_KEY, 'true')
    onDismiss()
  }

  function handleLocationRequest() {
    if ('geolocation' in navigator) {
      navigator.geolocation.getCurrentPosition(
        () => dismiss(),
        () => dismiss(), // dismiss on error too
      )
    } else {
      dismiss()
    }
  }

  return (
    <div className="onboarding-overlay" role="dialog" aria-modal="true" aria-label="Chào mừng">
      <div className="onboarding-modal">
        <div className="onboarding-modal__emoji">🌸</div>
        <h1 className="onboarding-modal__title">Chào mừng đến Huế</h1>
        <p className="onboarding-modal__subtitle">
          Cho phép truy cập vị trí để khám phá những địa điểm gần bạn nhất.
        </p>
        <button
          className="onboarding-modal__btn-primary"
          onClick={handleLocationRequest}
        >
          📍 Cho phép truy cập vị trí
        </button>
        <button
          className="onboarding-modal__btn-skip"
          onClick={dismiss}
        >
          Bỏ qua
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Wire OnboardingModal into HomePage**

Add to `src/pages/HomePage.tsx`:

At the top, add imports:
```tsx
import { useState } from 'react'
import { OnboardingModal, hasSeenOnboarding } from '../components/OnboardingModal'
```

Inside `HomePage`, add state and render:
```tsx
const [showOnboarding, setShowOnboarding] = useState(() => !hasSeenOnboarding())

// At the top of the returned JSX, before <div className="home-page">:
{showOnboarding && (
  <OnboardingModal onDismiss={() => setShowOnboarding(false)} />
)}
```

The full updated `src/pages/HomePage.tsx` return statement should start with:

```tsx
return (
  <>
    {showOnboarding && (
      <OnboardingModal onDismiss={() => setShowOnboarding(false)} />
    )}
    <div className="home-page">
      {/* ... rest unchanged ... */}
    </div>
  </>
)
```

- [ ] **Step 4: Verify in browser**

```bash
npm run dev
```

- On first visit (`/`): modal appears over Home
- "Cho phép" button requests GPS then closes modal
- "Bỏ qua" closes modal without GPS
- Refreshing the page: modal does NOT appear again
- Clear localStorage in DevTools and refresh: modal appears again

- [ ] **Step 5: Commit**

```bash
git add src/components/OnboardingModal.tsx src/components/OnboardingModal.css src/pages/HomePage.tsx
git commit -m "feat: add one-time OnboardingModal with GPS request"
```

---

## Task 15: My Trip page

**Files:**
- Modify: `src/pages/MyTripPage.tsx`
- Create: `src/pages/MyTripPage.css`

- [ ] **Step 1: Create MyTripPage CSS**

Create `src/pages/MyTripPage.css`:

```css
.my-trip-page {
  min-height: 100%;
  padding: var(--space-lg) var(--space-md);
}

.my-trip-page__title {
  font-family: var(--font-serif);
  font-size: 1.6rem;
  font-weight: 700;
  color: var(--color-on-surface);
  margin-bottom: var(--space-xl);
}

/* Empty state */
.my-trip-page__empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: var(--space-2xl) 0;
  text-align: center;
  gap: var(--space-md);
}

.my-trip-page__empty-icon {
  font-size: 4rem;
}

.my-trip-page__empty-text {
  font-family: var(--font-serif);
  font-size: 1.1rem;
  color: var(--color-on-surface-variant);
}

.my-trip-page__empty-hint {
  font-size: 0.85rem;
  color: var(--color-on-surface-variant);
  opacity: 0.7;
}

.my-trip-page__explore-btn {
  padding: 0.75rem var(--space-xl);
  background: linear-gradient(45deg, var(--color-primary), var(--color-primary-container));
  color: var(--color-on-primary);
  border: none;
  border-radius: var(--radius-lg);
  font-family: var(--font-sans);
  font-size: 0.9rem;
  font-weight: 700;
  cursor: pointer;
  -webkit-tap-highlight-color: transparent;
}

/* Saved list */
.my-trip-page__list {
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
  margin-bottom: var(--space-xl);
}

.my-trip-page__item {
  display: flex;
  align-items: center;
  gap: var(--space-md);
  background: var(--color-surface-container-lowest);
  border-radius: var(--radius-lg);
  padding: var(--space-sm);
  box-shadow: var(--shadow-card);
}

.my-trip-page__item-image {
  width: 72px;
  height: 72px;
  object-fit: cover;
  border-radius: var(--radius-md);
  flex-shrink: 0;
}

.my-trip-page__item-info {
  flex: 1;
  min-width: 0;
}

.my-trip-page__item-name {
  font-family: var(--font-serif);
  font-size: 0.95rem;
  font-weight: 700;
  color: var(--color-on-surface);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  margin-bottom: 0.15rem;
}

.my-trip-page__item-address {
  font-size: 0.72rem;
  color: var(--color-on-surface-variant);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.my-trip-page__item-actions {
  display: flex;
  flex-direction: column;
  gap: var(--space-xs);
  flex-shrink: 0;
}

.my-trip-page__item-btn {
  padding: 0.35rem 0.6rem;
  border-radius: var(--radius-sm);
  font-family: var(--font-sans);
  font-size: 0.7rem;
  font-weight: 600;
  cursor: pointer;
  border: none;
  -webkit-tap-highlight-color: transparent;
}

.my-trip-page__item-btn--directions {
  background: var(--color-surface-container);
  color: var(--color-on-surface);
}

.my-trip-page__item-btn--remove {
  background: none;
  color: var(--color-primary);
}

.my-trip-page__clear-btn {
  width: 100%;
  padding: 0.75rem;
  background: none;
  border: 1px solid var(--color-outline-variant);
  border-radius: var(--radius-lg);
  font-family: var(--font-sans);
  font-size: 0.85rem;
  color: var(--color-on-surface-variant);
  cursor: pointer;
  -webkit-tap-highlight-color: transparent;
}
```

- [ ] **Step 2: Implement MyTripPage**

Replace `src/pages/MyTripPage.tsx`:

```tsx
import { useNavigate } from 'react-router-dom'
import { useMyTrip } from '../hooks/useMyTrip'
import placesData from '../data/places.json'
import type { Place } from '../types'
import './MyTripPage.css'

const ALL_PLACES = placesData.places as Place[]

export function MyTripPage() {
  const navigate = useNavigate()
  const { savedIds, removePlace, clearAll } = useMyTrip()

  const savedPlaces = savedIds
    .map(id => ALL_PLACES.find(p => p.id === id))
    .filter((p): p is Place => p !== undefined)

  function handleDirections(place: Place) {
    const url = `https://www.google.com/maps/dir/?api=1&destination=${place.coordinates.lat},${place.coordinates.lng}`
    window.open(url, '_blank', 'noopener,noreferrer')
  }

  return (
    <div className="my-trip-page">
      <h1 className="my-trip-page__title">Hành trình của tôi</h1>

      {savedPlaces.length === 0 ? (
        <div className="my-trip-page__empty">
          <span className="my-trip-page__empty-icon">🗺️</span>
          <p className="my-trip-page__empty-text">Chưa có địa điểm nào được lưu</p>
          <p className="my-trip-page__empty-hint">
            Khám phá Di sản và nhấn 📌 để lưu những nơi bạn muốn ghé thăm.
          </p>
          <button
            className="my-trip-page__explore-btn"
            onClick={() => navigate('/heritage')}
          >
            Khám phá ngay
          </button>
        </div>
      ) : (
        <>
          <div className="my-trip-page__list">
            {savedPlaces.map(place => (
              <div key={place.id} className="my-trip-page__item">
                <img
                  src={place.coverImage}
                  alt={place.name}
                  className="my-trip-page__item-image"
                />
                <div className="my-trip-page__item-info">
                  <p className="my-trip-page__item-name">{place.name}</p>
                  <p className="my-trip-page__item-address">{place.address}</p>
                </div>
                <div className="my-trip-page__item-actions">
                  <button
                    className="my-trip-page__item-btn my-trip-page__item-btn--directions"
                    onClick={() => handleDirections(place)}
                    aria-label={`Đường đi đến ${place.name}`}
                  >
                    🧭
                  </button>
                  <button
                    className="my-trip-page__item-btn my-trip-page__item-btn--remove"
                    onClick={() => removePlace(place.id)}
                    aria-label={`Xóa ${place.name} khỏi hành trình`}
                  >
                    ❌
                  </button>
                </div>
              </div>
            ))}
          </div>

          <button
            className="my-trip-page__clear-btn"
            onClick={clearAll}
            aria-label="Xóa tất cả địa điểm đã lưu"
          >
            🗑️ Xóa tất cả
          </button>
        </>
      )}
    </div>
  )
}
```

- [ ] **Step 3: Verify end-to-end flow**

```bash
npm run dev
```

Full flow test:
1. Home → tap a place card → Details page opens
2. Tap "📌 Lưu" → button changes to "📌 Đã lưu"
3. Tap back arrow → return to previous screen
4. Navigate to "Hành trình" tab → saved place appears in list
5. Tap ❌ on the item → it disappears
6. Tap "Xóa tất cả" → empty state appears with "Khám phá ngay" button
7. Tap "Khám phá ngay" → navigates to Heritage tab
8. Refresh page → trip persists (localStorage)

- [ ] **Step 4: Run full test suite**

```bash
npm test
```

Expected: 11 tests pass.

- [ ] **Step 5: Final build check**

```bash
npm run build
```

Expected: build succeeds with no TypeScript errors.

- [ ] **Step 6: Commit**

```bash
git add src/pages/MyTripPage.tsx src/pages/MyTripPage.css
git commit -m "feat: implement My Trip page with empty state and saved places list"
```

---

## Summary

| Task | Feature | Key files |
|------|---------|-----------|
| 1 | Setup & cleanup | `vite.config.ts`, `package.json`, `CLAUDE.md` |
| 2 | Types | `src/types/index.ts` |
| 3 | Static data | `src/data/places.json` |
| 4 | Design tokens | `src/index.css` |
| 5 | Routing shell | `src/App.tsx`, `src/components/AppLayout.tsx` |
| 6 | BottomNav | `src/components/BottomNav.tsx` |
| 7 | useMyTrip hook | `src/hooks/useMyTrip.ts` + tests |
| 8 | PlaceCard | `src/components/PlaceCard.tsx` |
| 9 | CategoryFilter | `src/components/CategoryFilter.tsx` |
| 10 | Heritage page | `src/pages/HeritagePage.tsx` |
| 11 | Home page | `src/pages/HomePage.tsx` |
| 12 | ImageGallery | `src/components/ImageGallery.tsx` |
| 13 | Details page | `src/pages/DetailsPage.tsx` |
| 14 | OnboardingModal | `src/components/OnboardingModal.tsx` |
| 15 | My Trip page | `src/pages/MyTripPage.tsx` |
